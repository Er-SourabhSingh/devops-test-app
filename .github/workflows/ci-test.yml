name: CI Test

on:
  push:
    branches: [ 'bug/*', 'feature/*' ]

jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: echo "Build passed successfully"
